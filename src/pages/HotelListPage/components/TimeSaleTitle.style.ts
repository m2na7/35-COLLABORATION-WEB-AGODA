import { css } from '@emotion/react';
import theme from '@styles/theme';

const timeSaleTitleWrapper = css`
  display: flex;
  align-items: center;
  padding: 0.6rem 1.6rem 0.3rem 1.6rem;
  gap: 0.6rem;
  ${theme.font.body1_b_14};
`;

export { timeSaleTitleWrapper };
